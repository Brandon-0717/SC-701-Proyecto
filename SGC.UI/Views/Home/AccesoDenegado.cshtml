@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container bg-secondary d-flex align-items-center justify-content-center py-5">
    <div class="card  border-0 text-center">
        <div class="card-body p-4">

            <div class="mb-3 text-danger">
                <i class="bi bi-shield-lock fs-1"></i>
            </div>

            <h1 class="fw-bold text-danger mb-2">401</h1>

            <h5 class="fw-semibold mb-3">Acceso denegado</h5>

            <p class="text-muted mb-4">
                No tienes los permisos necesarios para acceder a esta página.
            </p>

            <a asp-controller="Home" asp-action="Index" class="btn btn-primary w-100">
                <i class="bi bi-house-door me-1"></i>
                Volver al inicio
            </a>

        </div>
    </div>
</div>
